# 🗃️ Система логирования изменений (Activity Log)

**Дата актуализации:** 05.12.2024  
**Версия:** 1.0  
**Статус:** ✅ Работает в production

## 🎯 Цели системы

1. **Аудит изменений** - отслеживание кто, когда и что изменил в системе
2. **Восстановление истории** - возможность просмотреть историю изменений объекта
3. **Отладка проблем** - анализ изменений при возникновении проблем
4. **Юридические требования** - соблюдение требований для финансовых операций

## 🏗️ Архитектура

### Технологический стек
- **Библиотека:** Spatie Laravel Activitylog v4.10
- **Хранение:** MySQL таблица `activity_log`
- **Интерфейс:** Filament Resource (`ActivityLogResource`)
- **Очистка:** Ежедневное задание + команды ручного управления

### Модели с логированием (приоритет)

#### ✅ Реализовано:
1. **Assignment** - назначения (статусы, исполнители, даты)
2. **Shift** - смены (финансы, время работы, статусы)
3. **WorkRequest** - заявки на работы (жизненный цикл)
4. **Compensation** - компенсации к сменам (финансовые операции)
5. **ShiftExpense** - операционные расходы смен (финансы)
6. **ContractorRate** - ставки подрядчиков (финансы)

#### 📋 В планах:
7. MassPersonnelReport - отчеты по массовому персоналу
8. User - изменения профилей пользователей
9. Candidate - процесс подбора персонала

## 📊 Структура таблицы `activity_log`

```sql
CREATE TABLE `activity_log` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `log_name` varchar(255) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `description` text COLLATE utf8mb4_unicode_ci NOT NULL,
  `subject_type` varchar(255) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `event` varchar(255) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `subject_id` bigint unsigned DEFAULT NULL,
  `causer_type` varchar(255) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `causer_id` bigint unsigned DEFAULT NULL,
  `properties` json DEFAULT NULL,
  `batch_uuid` char(36) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `subject` (`subject_type`,`subject_id`),
  KEY `causer` (`causer_type`,`causer_id`),
  KEY `activity_log_log_name_index` (`log_name`),
  KEY `activity_log_created_at_index` (`created_at`),
  KEY `activity_log_event_index` (`event`),
  KEY `subject_type_event_created_at_index` (`subject_type`,`event`,`created_at`),
  KEY `activity_log_batch_uuid_index` (`batch_uuid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**🛠️ Реализация логирования в моделях**

**Базовый шаблон**

php

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

use Spatie\Activitylog\LogOptions;

use Spatie\Activitylog\Traits\LogsActivity;

use Spatie\Activitylog\Traits\CausesActivity;

class YourModel extends Model

{

`    `use LogsActivity, CausesActivity;

`    `protected $fillable = [*/\* ... \*/*];

`    `*// === ЛОГИРОВАНИЕ ===*

`    `public function getActivitylogOptions(): LogOptions

`    `{

`        `return LogOptions::defaults()

`            `->logOnly([*/\* поля для логирования \*/*])

`            `->logOnlyDirty()

`            `->dontSubmitEmptyLogs()

`            `->dontLogIfAttributesChangedOnly(['updated\_at'])

`            `->setDescriptionForEvent(function(string $eventName) {

`                `return match($eventName) {

`                    `'created' => 'Объект создан',

`                    `'updated' => 'Объект изменен',

`                    `'deleted' => 'Объект удален',

`                    `'restored' => 'Объект восстановлен',

`                    `default => "Объект {$eventName}",

`                `};

`            `})

`            `->useLogName('your\_model')

`            `->logFillable()

`            `->submitEmptyLogs(false);

`    `}



`    `public function tapActivity(\Spatie\Activitylog\Models\Activity $activity, string $eventName)

`    `{

`        `$activity->properties = $activity->properties->merge([

`            `*// Дополнительная информация для логов*

`        `]);

`    `}

}

**Особенности для финансовых моделей**

Для финансовых моделей (Compensation, ShiftExpense, ContractorRate) добавляются:

- Форматирование сумм (number\_format($amount, 2) . ' ₽')
- Флаг 'financial\_operation' => true
- Отображение статусов на русском

**🖥️ Интерфейс Filament**

**ActivityLogResource**

- **Путь:** /admin/activity-logs
- **Группа:** "Система"
- **Права доступа:** admin, manager, hr
- **Фильтрация:** по типу объекта, событию, дате, пользователю
- **Детальный просмотр:** через модальное окно с разметкой изменений

**Иконки для типов объектов**

- 📋 Назначение (Assignment)
- 👤 Пользователь (User)
- 💰 Смена (Shift)
- 📄 Заявка (WorkRequest)
- 💸 Компенсация (Compensation)
- 🧾 Расход смены (ShiftExpense)
- 💰 Ставка подрядчика (ContractorRate)
- 🏢 Подрядчик (Contractor)

**⚙️ Управление и обслуживание**

**Команды Artisan**

|Команда|Описание|Пример|
| :- | :- | :- |
|activity:manage stats|Статистика системы логов|sail artisan activity:manage stats|
|activity:manage cleanup|Очистка старых логов|sail artisan activity:manage cleanup --days=365|
|activity:manage optimize|Оптимизация таблицы|sail artisan activity:manage optimize|
|logs:test-indexes|Тестирование индексов|sail artisan logs:test-indexes|
|activity:manage cleanup --dry-run|Тестовый прогон очистки|sail artisan activity:manage cleanup --days=30 --dry-run|

**Планировщик (AppServiceProvider)**

php

*// App\Providers\AppServiceProvider.php*

public function boot(): void

{

`    `*// Очистка логов - ежедневно в 3:00 (используем нашу новую команду)*

`    `$this->callAfterResolving(Schedule::class, function (Schedule $schedule) {

`        `*// Основная очистка старых логов*

`        `$schedule->command('activity:manage cleanup --days=365')

`                 `->dailyAt('03:00')

`                 `->onOneServer()

`                 `->runInBackground()

`                 `->appendOutputTo(storage\_path('logs/activity-cleanup.log'));



`        `*// Оптимизация таблицы - еженедельно в воскресенье*

`        `$schedule->command('activity:manage optimize')

`                 `->weeklyOn(0, '04:00') *// Воскресенье в 4:00*

`                 `->onOneServer()

`                 `->runInBackground();



`        `*// Статистика - ежедневно в 6:00*

`        `$schedule->command('activity:manage stats')

`                 `->dailyAt('06:00')

`                 `->onOneServer()

`                 `->runInBackground()

`                 `->appendOutputTo(storage\_path('logs/activity-stats.log'));

`    `});

}

**📈 Производительность**

**Индексы для оптимизации**

1. activity\_log\_created\_at\_index - фильтрация по дате
1. activity\_log\_event\_index - фильтрация по событию
1. subject\_type\_event\_created\_at\_index - составной индекс для частых запросов
1. activity\_log\_batch\_uuid\_index - поиск по batch\_uuid

**Ограничения хранения**

- **Период хранения:** 365 дней
- **Ежедневная очистка:** автоматически в 3:00
- **Резервное копирование:** не предусмотрено (при необходимости - экспорт)

**Ожидаемая нагрузка**

|Показатель|Значение|
| :- | :- |
|Записей в год|~100,000|
|Средний размер записи|~1 КБ|
|Общий объем в год|~100 МБ|
|Время ответа при фильтрации|< 100 мс|

**🔍 Мониторинг**

**Логирование операций**

- Канал activity (storage/logs/activity/activity.log)
- Канал activity\_errors (storage/logs/activity/errors.log)
- Ротация: 30 дней для activity, 90 дней для errors

**Метрики**

1. Количество записей по типам объектов
1. Количество операций по событиям
1. Объем данных в таблице
1. Время выполнения запросов

**🚀 Интеграция с бизнес-процессами**

**Workflow логирования**

1. **Создание объекта** → запись в лог с типом created
1. **Изменение объекта** → запись с типом updated и diff изменений
1. **Удаление объекта** → запись с типом deleted
1. **Восстановление** → запись с типом restored

**Особые сценарии**

- **Финансовые операции:** выделение цветом и форматированием сумм
- **Изменение статусов:** визуальное выделение в интерфейсе
- **Массовые операции:** группировка через batch\_uuid

**Особенности модели ShiftExpense**

- **Типы расходов:** Такси, Материалы, Питание, Проживание, Прочие
- **Пользовательские типы:** возможность создания через createCustomType()
- **Гибкое управление:** методы getAllTypes(), getCustomTypes()

**📝 Чек-лист внедрения для новой модели**

- Добавить трейты LogsActivity и CausesActivity
- Реализовать метод getActivitylogOptions()
- Настроить метод tapActivity() при необходимости
- Добавить тип модели в ActivityLogResource
- Обновить фильтры в ActivityLogResource
- Протестировать создание/изменение/удаление
- Проверить отображение в интерфейсе

**🔄 Обратная совместимость**

**При обновлении системы**

1. Существующие данные не затрагиваются
1. Новые модели добавляют логирование постепенно
1. Интерфейс адаптируется под новые типы объектов

**При откате**

1. Логи сохраняются в базе данных
1. Интерфейс Filament продолжает работать
1. Команды управления требуют наличия таблицы

**📚 Полезные ссылки**

- [Документация Spatie Activitylog](https://spatie.be/docs/laravel-activitylog/v4)
- [Filament Tables Documentation](https://filamentphp.com/docs/3.x/tables/table)
- [Laravel Scheduling](https://laravel.com/docs/12.x/scheduling)
-----
*Документ обновляется при изменении системы логирования.*