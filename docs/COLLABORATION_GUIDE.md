# 👥 Руководство по совместной работе

## 🛡️ Защита основной ветки

Ветка `main` защищена следующими правилами:
- ✅ **Require PR** - запрещены прямые пуши в main
- ✅ **Require approval** - нужен 1 approval перед мержем  
- ✅ **Dismiss stale approvals** - новые коммиты сбрасывают approval
- ✅ **Conversation resolution** - все комментарии должны быть resolved
- ❌ **Force pushes запрещены** - даже для администратора
- ❌ **Удаление ветки запрещено** - даже для администратора

## 🔄 Workflow процесса

### Для разработчика:
```bash
# 1. Создать ветку от актуального main
git checkout main
git pull origin main
git checkout -b feature/краткое-описание

# 2. Работать и коммитить
git add .
git commit -m "feat: описание изменений"
git push origin feature/краткое-описание

# 3. Создать Pull Request через GitHub UI
```

### Для ревьювера (владельца):

1. **Получить уведомление** о новом PR

2. **Проверить код** - файлы, коммиты, описание

3. Оставить комментарии если нужны исправления

4. Нажать "Approve" когда все готово

5. Смержить PR после approval

## 💬 Процесс code review

### Требования перед мержем:

- [ ] Все комментарии отмечены как "Resolved"

* Получен минимум 1 approval

* Нет конфликтов с main веткой

### Как разрешать комментарии:

1. Разработчик исправляет замечания

2. Делает коммит и пушит изменения

3. В PR отмечает комментарий кнопкой "Resolve conversation"

## 🚨 Важные моменты

### Если нужно обновить PR после approval:

* Добавьте новые коммиты в ветку

* Approval автоматически сбросится

* Нужно новое review и approval

### Если есть конфликты с main:
```bash
git checkout feature/ваша-ветка
git fetch origin
git merge origin/main
# Решить конфликты, затем
git push origin feature/ваша-ветка
```

## 📞 Контакты для вопросов

* Владелец: @Nick-Major

* По техническим вопросам - создавайте issue в репозитории
