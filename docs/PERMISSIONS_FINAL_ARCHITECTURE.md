# 🏗️ ФИНАЛЬНАЯ АРХИТЕКТУРА РАЗРЕШЕНИЙ

## 🎯 **ПРИНЦИПЫ:**

1. **ЕДИНЫЙ ИСТОЧНИК ИСТИНЫ:** Filament Shield
2. **БЕЗ ДУБЛИРОВАНИЯ:** Никаких can методов в ресурсах
3. **ПРОСТОТА:** Gates только для нестандартных действий
4. **АВТОМАТИЗАЦИЯ:** Laravel сам находит политики для стандартных методов

## 🛡️ **Filament Shield - ОСНОВА:**

### **Что делает:**
- Создает 501 разрешение автоматически
- Связывает разрешения с ролями
- Автоматически проверяет доступ к ресурсам
- Управляется через админку Filament

### **Как работает:**
1. Пользователь пытается открыть ресурс
2. Filament проверяет `view_any_{resource}` разрешение
3. Если нет - ресурс скрыт в навигации
4. Если есть - показывает ресурс

## 🔐 **AuthServiceProvider - МИНИМАЛЬНЫЙ:**

```php
protected $policies = [
    // Только для встроенных моделей Laravel
    \Illuminate\Notifications\DatabaseNotification::class => \App\Policies\DatabaseNotificationPolicy::class,
];

public function boot(): void
{
    $this->registerPolicies();
    
    // Администратор может всё
    Gate::before(fn ($user) => $user->hasRole('admin') ? true : null);
    
    // ТОЛЬКО нестандартные действия
    Gate::define('confirm_assignment', [AssignmentPolicy::class, 'confirm']);
    Gate::define('reject_assignment', [AssignmentPolicy::class, 'reject']);
    
    // ❌ НИКАКИХ ДРУГИХ GATES!
}
```
