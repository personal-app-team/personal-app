## ✅ ОБНОВЛЕНИЕ SHIFT EXPENSE И ДОКУМЕНТАЦИИ (05.12.2024)

### Выполнено:
- [x] Обновлена модель ShiftExpense с типами расходов:
  - Добавлены стандартные типы: taxi, materials, food, accommodation, other
  - Реализована поддержка пользовательских типов (custom_type)
  - Созданы методы для управления типами: getCustomTypes(), createCustomType()
  - Добавлены скоупы для фильтрации по типам
- [x] Создана миграция для добавления поля `custom_type`
- [x] Обновлена документация ACTIVITYLOG_SYSTEM.md:
  - Исправлен раздел о планировщике (AppServiceProvider вместо Kernel)
  - Добавлена информация о ShiftExpense с типами
  - Обновлен список реализованных моделей

### Особенности ShiftExpense:
1. **Стандартные типы:**
   - `taxi` - Такси
   - `materials` - Материалы
   - `food` - Питание
   - `accommodation` - Проживание
   - `other` - Прочие расходы

2. **Пользовательские типы:**
   - Хранятся в кэше Redis
   - Добавляются через `ShiftExpense::createCustomType()`
   - Отображаются с иконкой 📝

3. **Методы для работы:**
   - `getAllTypes()` - все типы (стандартные + пользовательские)
   - `getCustomTypes()` - только пользовательские
   - `isCustomType()` - проверка типа

### Команды для проверки:
```bash
# Запустить миграцию
sail artisan migrate

# Тест типов ShiftExpense
sail tinker
>>> ShiftExpense::createCustomType('Грузоперевозки', 'Перевозка материалов');
>>> $types = ShiftExpense::getAllTypes();
>>> print_r($types);

# Проверка документации
cat docs/ACTIVITYLOG_SYSTEM.md | grep -A5 "Планировщик"
```
