# 🚨 TODO и Исправления (15.12.2025)

## ✅ ВЫПОЛНЕНО СЕГОДНЯ:

### 🔧 **Перестройка системы ставок подрядчиков (ContractorRate)**

1. **Создана новая структура таблицы `contractor_rates`:**
   - `contractor_id` → Подрядчик
   - `category_id` → Категория работ
   - `specialty_name` → Название специальности подрядчика
   - `hourly_rate` → Ставка (руб/час)
   - `rate_type` → Тип ставки ('mass' или 'personalized')
   - `is_anonymous` → Обезличенный персонал
   - `is_active` → Активная ставка

2. **Обновлены связанные таблицы:**
   - `shifts` → добавлен `contractor_rate_id`
   - `mass_personnel_reports` → добавлен `contractor_rate_id`

3. **Обновлены модели:**
   - `ContractorRate` → полная переработка
   - `ContractorRateResource` → новая форма и логика

### 🎯 **Разделение логики специальностей:**
- **Specialty (специальности)** → только для НАШИХ исполнителей
- **ContractorRate (ставки подрядчиков)** → отдельные специальности для подрядчиков с привязкой к категории

## 🔴 ТРЕБУЕТ ПРОВЕРКИ И ИСПРАВЛЕНИЙ:

### 1. **Shift (Смены)**
   - [ ] Проверить метод `determineBaseRate()` в модели Shift
   - [ ] Обновить ShiftResource для работы с `contractor_rate_id`
   - [ ] Проверить расчеты для подрядчиков (персонализированные и массовые)

### 2. **MassPersonnelReport (Отчеты по массовому персоналу)**
   - [ ] Проверить метод `determineBaseRate()` 
   - [ ] Обновить связи с ContractorRate
   - [ ] Проверить расчеты для массового персонала

### 3. **Category (Категории)**
   - [ ] Проверить методы для работы с подрядчиками
   - [ ] Обновить связи (через ContractorRate)
   - [ ] Проверить фильтрацию подрядчиков по категориям

### 4. **Specialty (Специальности)**
   - [ ] Убрать связь с contractor_rates (если осталась)
   - [ ] Обновить методы, которые предполагали связь с подрядчиками
   - [ ] Проверить, что специальности используются только для наших исполнителей

### 5. **Contractor (Подрядчики)**
   - [ ] Обновить методы для работы с новыми ставками
   - [ ] Проверить связи через ContractorRate
   - [ ] Обновить ContractorResource (добавить вкладку с ставками)

### 6. **WorkRequest (Заявки на работы)**
   - [ ] Проверить логику выбора подрядчиков по категориям
   - [ ] Обновить фильтрацию доступных подрядчиков

## 🔗 **ИНТЕГРАЦИОННЫЕ ТОЧКИ ДЛЯ ПРОВЕРКИ:**

### **Создание смены:**
- [ ] Для наших исполнителей: ставка из `user_specialties` или `specialties.base_hourly_rate`
- [ ] Для подрядчиков: ставка из `contractor_rates.hourly_rate`

### **Создание отчета по массовому персоналу:**
- [ ] Ставка берется из `contractor_rates` с `rate_type = 'mass'`
- [ ] Категория и специальность подрядчика из `contractor_rates`

### **Фильтрация подрядчиков при создании заявки:**
- [ ] По категории работ
- [ ] По наличию активных ставок в категории

## 🧪 **ТЕСТОВЫЕ СЦЕНАРИИ ДЛЯ ПРОВЕРКИ:**

1. **Сценарий 1: Создание ставки подрядчика**
   - Выбор подрядчика → Выбор категории → Ввод названия специальности → Установка ставки

2. **Сценарий 2: Создание смены для подрядчика**
   - Создание заявки → Назначение подрядчика → Создание смены → Проверка расчета ставки

3. **Сценарий 3: Отчет по массовому персоналу**
   - Создание заявки с массовым персоналом → Создание отчета → Проверка расчета по массовой ставке

4. **Сценарий 4: Наши исполнители**
   - Создание специальности → Назначение исполнителю → Создание смены → Проверка расчета

## 📊 **ОСТАВШИЕСЯ МИГРАЦИИ (если не выполнены):**

- [ ] Миграция для удаления связи contractor_rates → specialties (уже сделано)
- [ ] Миграция для добавления contractor_rate_id в shifts (уже сделано)
- [ ] Миграция для обновления mass_personnel_reports (уже сделано)

## 🚨 **ВОЗМОЖНЫЕ ПРОБЛЕМЫ:**

1. **Ошибки в существующих данных:** Проверить, что нет записей, ссылающихся на старую структуру
2. **API endpoints:** Проверить API контроллеры на использование старых связей
3. **Filament Resources:** Обновить все RelationManagers, которые используют старые связи

## 📅 **ПЛАН НА СЛЕДУЮЩИЙ ЭТАП:**

1. **Завтра (16.12):** Проверка и обновление моделей Shift и MassPersonnelReport
2. **17.12:** Обновление Category и Specialty моделей
3. **18.12:** Тестирование полных workflow
4. **19.12:** Исправление найденных ошибок и документирование

### ✅ **Исправление структуры после анализа (16.12.2025)**

**Обнаружено:**
1. В таблице `work_requests` нет поля `specialty_id` - это правильно
2. Заявки на работы определяют только категорию, конкретная специальность определяется на уровне смены
3. Связь `Specialty->workRequests()` некорректна и должна быть удалена

**Выполнено:**
- [x] Удалена связь `workRequests()` из модели `Specialty`
- [x] Убран столбец `work_requests_count` из `SpecialtyResource`
- [ ] Обновлен метод `determineBaseRate()` в `Shift` (убрать устаревшую логику для подрядчиков без `contractor_rate_id`)

**Требуется проверить:**
1. Как создаются смены для наших исполнителей - откуда берется `specialty_id`?
2. Все ли смены для подрядчиков имеют `contractor_rate_id`?
3. Корректно ли работает создание `MassPersonnelReport` для массового персонала?

**Обновленный workflow:**

1. **Заявка на работы:**
   - Выбирается `category_id` (категория работ)
   - Выбирается `work_type_id` (тип работ)
   - Указывается `address_id`, `project_id`, `purpose_id`

2. **Создание смены (наши исполнители):**
   - Берется `specialty_id` из назначенного исполнителя (user_specialties)
   - Ставка определяется через `user_specialties.pivot.base_hourly_rate`

3. **Создание смены (подрядчики):**
   - Берется `contractor_rate_id` (ставка подрядчика для категории)
   - Ставка определяется через `contractor_rates.hourly_rate`

4. **Создание отчета (массовый персонал):**
   - Берется `contractor_rate_id` с `rate_type='mass'`
   - Ставка определяется через `contractor_rates.hourly_rate`
