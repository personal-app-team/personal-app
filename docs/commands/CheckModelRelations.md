# 📋 Команда: app:check-model-relations

## Описание
Проверяет модели на устаревшие отношения, которые могли остаться после рефакторинга базы данных.

## Что проверяет
1. Отношения `hasMany` или `belongsTo` к модели `User` с полями `contract_type_id` или `tax_status_id`
2. Эти поля были перенесены в таблицу `employment_history`

## Использование
```bash
sail artisan app:check-model-relations
```

```text
🔍 Проверка моделей на устаревшие отношения...
✅ Проверено моделей: 41
❌ Найдены потенциальные проблемы в моделях:
  📁 TaxStatus: Отношение к User с устаревшими полями contract_type_id или tax_status_id
```

## Модели для проверки

* TaxStatus

* Contractor (может иметь contract_type_id/tax_status_id)

* Shift (проверить tax_status_id)

## Рекомендации

**Если найдены проблемы, нужно:**

1. Обновить отношения в моделях

2. Проверить миграции

3. Обновить Filament Resources