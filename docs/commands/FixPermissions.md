
### 3. Документация для `permissions:fix`

```markdown
# 📋 Команда: permissions:fix

## Описание
Исправляет права доступа для администратора. Назначает все разрешения роли `admin` и назначает эту роль пользователю `admin@example.com`.

## Что делает команда
1. Ищет пользователя с email `admin@example.com`
2. Создает/проверяет роль `admin`
3. Назначает ВСЕ разрешения роли `admin`
4. Назначает роль пользователю
5. Проверяет результат

## Использование
```bash
sail artisan permissions:fix
```

## Требования

1. Должен существовать пользователь admin@example.com

2. Должны быть созданы разрешения через app:list-permissions --generate


## Пример вывода
```text
🔧 Исправление прав доступа...
👤 Найден администратор: admin@example.com
🎭 Роль 'admin' проверена
🔑 Роли 'admin' назначено 451 разрешение
👤 Пользователю admin@example.com назначена роль admin

📊 Результат:
  • Роли: admin
  • Разрешений: 451

🔍 Проверка доступа к ресурсам:
  • WorkRequest          : ✅
  • User                 : ✅
  • Contractor           : ✅
  • Shift                : ✅
  • Assignment           : ✅

🎉 Права доступа исправлены!
```

## Важно

* Команда использует syncPermissions() - заменяет все разрешения роли

* Рекомендуется запускать после создания новых моделей
