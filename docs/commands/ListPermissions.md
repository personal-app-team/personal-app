
### 2. Документация для `app:list-permissions`

```markdown
# 📋 Команда: app:list-permissions

## Описание
Показывает все разрешения системы Spatie и их описание. Может генерировать недостающие разрешения.

## Функционал

1. Показывает все существующие разрешения
2. Анализирует соответствие разрешений моделям
3. Генерирует недостающие разрешения (с опцией `--generate`)

## Использование
```bash
# Показать все разрешения
sail artisan app:list-permissions

# Сгенерировать недостающие разрешения
sail artisan app:list-permissions --generate
```

## Структура разрешений

Для каждой модели генерируются 11 разрешений:

* view_any_{model}

* view_{model}

* create_{model}

* update_{model}

* delete_{model}

* delete_any_{model}

* restore_{model}

* restore_any_{model}

* force_delete_{model}

* force_delete_any_{model}

* replicate_{model}


## Пример вывода

```text
📋 СИСТЕМА РАЗРЕШЕНИЙ
======================

🔍 Существующие разрешения в базе:
• view_any_user
• view_user
...

📊 Анализ по моделям:
Модель            Найдено  Отсутствует  Статус
User              11       0            ✅ Полный
WorkRequest       8        3            ⚠️ Неполный
```

## Интеграция с Filament

Разрешения автоматически связываются с Filament Shield для управления доступом.
