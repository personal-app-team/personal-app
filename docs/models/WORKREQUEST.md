cat > docs/WORKREQUEST.md << 'EOF'
# WorkRequest - Заявки на работы

## 📋 Общая информация
WorkRequest - основная сущность для учета работ. Заявка создается для выполнения работ в определенную дату, по указанному адресу, с нужным количеством исполнителей.

## 🏗️ Архитектура

### Поля таблицы `work_requests`:
- `id` - уникальный идентификатор
- `request_number` - номер заявки (генерируется автоматически)
- `external_number` - внешний номер для интеграций
- `initiator_id` - инициатор заявки (пользователь, создавший заявку)
- `brigadier_id` - бригадир (ответственный на объекте)
- `contact_person` - контактное лицо (если нет бригадира)
- `workers_count` - количество требуемых людей
- `desired_workers` - желаемые исполнители (для подрядчиков)
- `estimated_duration_minutes` - плановая продолжительность в минутах
- `work_date` - дата выполнения работ
- `start_time` - время начала работ
- `additional_info` - дополнительная информация
- `status` - статус заявки
- `dispatcher_id` - ответственный диспетчер
- `work_type_id` - вид работ
- `address_id` - официальный адрес
- `is_custom_address` - флаг нестандартного адреса
- `custom_address` - адрес вручную
- `category_id` - категория специалистов
- `total_worked_hours` - общее отработанное время
- `project_id` - проект
- `purpose_id` - назначение/цель
- `contractor_id` - подрядчик
- `personnel_type` - тип персонала (our_staff/contractor)
- `published_at`, `staffed_at`, `completed_at` - даты статусов
- `created_at`, `updated_at`, `deleted_at` - временные метки

## 🔄 Workflow заявки

### 1. Создание заявки:
- **Кто может создавать**: 
  - Администратор - может указать любого инициатора
  - Диспетчер - автоматически становится инициатором
  - Инициатор с правами бригадира - автоматически становится инициатором
- **Поле `initiator_id`**: 
  - Для администратора: выбирается из списка
  - Для других ролей: автоматически устанавливается `auth()->id()`
- **Номер заявки**: генерируется автоматически по шаблону `WR-{префикс}-{год}-{порядковый}`
  - Для подрядчиков: префикс = contractor_code подрядчика
  - Для наших исполнителей: префикс = префикс категории

### 2. Взятие в работу диспетчером:
- **Условия**:
  - Статус заявки: `published`
  - Пользователь имеет роль `dispatcher`
- **Что происходит**:
  - Статус меняется на `in_progress`
  - Поле `dispatcher_id` заполняется ID текущего пользователя
  - В логах фиксируется изменение
- **Интерфейс**: кнопка "Взять в работу" в таблице заявок

### 3. Назначение исполнителей:
- **Текущий способ**: 
  - Кнопка "Назначить исполнителей" открывает AssignmentResource с фильтром по заявке
  - Диспетчер создает назначения типа `work_request`
- **Планируемое улучшение**:
  - Модальное окно создания назначения прямо из заявки
  - Предзаполненные поля: заявка, тип назначения
  - Удобный интерфейс выбора исполнителей

### 4. Мониторинг подтверждений:
- **Индикаторы в таблице**:
  - Колонка `assignments_summary`: ✓2 ⏳1 📋3
  - Цветовая индикация: зеленый (все подтверждены), желтый (часть), красный (нет)
  - Подсказка с детальной статистикой

### 5. Статусные переходы:

published → in_progress → closed → working → completed
↓ ↓ ↓ ↓ ↓
→ no_shifts → unclosed → cancelled


## 👥 Ролевая модель

### Администратор (`admin`):
- ✅ Полный доступ ко всем заявкам
- ✅ Может изменять поле "Инициатор заявки"
- ✅ Может удалять любые заявки
- ✅ Видит все заявки в системе

### Диспетчер (`dispatcher`):
- ✅ Может создавать заявки (становится инициатором)
- ✅ Может брать в работу заявки со статусом `published`
- ✅ Может назначать исполнителей на свои заявки
- ✅ Редактирует только свои заявки (где `dispatcher_id` = его ID)
- ❌ Не может удалять заявки
- ❌ Не может изменять инициатора у существующих заявок

### Фильтры для диспетчеров:
- "Назначенные мне" - заявки, где пользователь диспетчер
- "Доступные для взятия" - заявки со статусом `published`

## 🎯 Новый функционал (добавлен 18.12.2025)

### ✅ Действие "Взять в работу" для диспетчеров:
- Кнопка появляется только у заявок со статусом `published`
- Доступна только пользователям с ролью `dispatcher`
- При нажатии вызывает метод `takeInProgress()` из модели
- Записывает `dispatcher_id` текущего пользователя
- Меняет статус на `in_progress`
- Показывает уведомление об успехе

### ✅ Действие "Назначить исполнителей":
- Открывает страницу AssignmentResource с фильтром по текущей заявке
- Доступно только для диспетчера, который взял заявку в работу
- Открывается в новой вкладке для удобства

### ✅ Индикаторы назначений в таблице:
- Колонка `assignments_summary` показывает:
  - ✓ Подтвержденные назначения
  - ⏳ Ожидающие подтверждения  
  - 📋 Всего назначений
- Цветовая индикация:
  - Зеленый - все подтверждены
  - Желтый - есть подтвержденные, но не все
  - Красный - никто не подтвердил
- Подсказка с детальной статистикой

### ✅ Фильтры для диспетчеров:
- "Назначенные мне" - только заявки, где текущий пользователь диспетчер
- "Доступные для взятия" - заявки со статусом `published`

### ✅ Улучшенное отображение:
- Номер заявки показывает дату работ в описании
- Статусы переведены на русский язык
- Интуитивная цветовая схема для статусов

### ✅ Логика доступа:
- Диспетчер может редактировать только свои заявки
- Админ может редактировать все
- Удаление только для администраторов

## 🔧 Технические детали

### Генерация номера заявки:
```php
public function generateRequestNumber(): string
{
    $prefix = $this->getRequestNumberPrefix(); // contractor_code или category->prefix
    $year = now()->year;
    $sequence = // получаем последний номер за год + 1
    return sprintf("WR-%s-%d-%04d", $prefix, $year, $sequence);
}
```

### Техническая справка по initiator_id:

**Связь в БД:**
- Поле: `initiator_id BIGINT UNSIGNED`
- Внешний ключ: `work_requests_initiator_id_foreign`
- Ссылается на: `users(id)`
- Действия при удалении: `NO ACTION` (не удалять заявку при удалении пользователя)

**Связь в модели:**
```php
// В WorkRequest модели:
public function initiator()
{
    return $this->belongsTo(User::class, 'initiator_id');
}

// В User модели:
public function initiatedRequests()
{
    return $this->hasMany(WorkRequest::class, 'initiator_id');
}
```

## **Валидация бизнес-правил:**

1. Один тип персонала (либо наши, либо подрядчик)

2. Категория обязательна

3. Бригадир или контактное лицо

4. Дата работ обязательна

5. Адрес (официальный или кастомный)

6. Проверка, что бригадир назначен на дату


## **Логирование:**

* Используется Spatie Activitylog

* Логируются все изменения статусов и важных полей

* Автоматическое описание событий на русском языке


## **🚀 Следующие шаги развития**


### Высокий приоритет:

1. Модальное окно назначения исполнителей - создание назначения прямо из заявки

2. Настройка разрешений - проверка прав диспетчеров на создание назначений

3. Интеграция с уведомлениями - оповещения о новых назначениях


### Средний приоритет:

4. Массовое назначение - назначение нескольких исполнителей одной операцией

5. Шаблоны заявок - быстрый дубль часто используемых заявок

6. Календарь заявок - визуализация в виде календаря


### **Низкий приоритет:**

7. Импорт заявок - из Excel/CSV

8. Автоматическое распределение - алгоритм подбора исполнителей

9. Мобильный интерфейс - упрощенный просмотр для бригадиров


## **📊 Связи с другими сущностями**


### **Прямые связи:**

* initiator → User (инициатор)

* brigadier → User (бригадир)

* dispatcher → User (диспетчер)

* contractor → Contractor (подрядчик)

* category → Category (категория)

* workType → WorkType (вид работ)

* project → Project (проект)

* purpose → Purpose (назначение)

* address → Address (адрес)


### **Зависимые сущности:**

* shifts ← Shift (смены по заявке)

* assignments ← Assignment (назначения на заявку)

* massPersonnelReports ← MassPersonnelReport (отчеты по массовому персоналу)

* statusHistory ← WorkRequestStatus (история статусов)

## **❓ Частые вопросы**

**Q: Кто может создавать заявки?**
A: Администраторы, диспетчеры и инициаторы-бригадиры.

**Q: Как изменить инициатора заявки?**
A: Только администратор может изменить поле "Инициатор заявки".

**Q: Почему не вижу кнопку "Взять в работу"?**
A: Проверьте: 1) Ваша роль - dispatcher, 2) Заявка в статусе published.

**Q: Как назначить исполнителей?**
A: 1) Возьмите заявку в работу, 2) Нажмите "Назначить исполнителей", 3) Создайте назначения.

**Q: Где видеть статус подтверждений?**
A: В колонке "Назначения" таблицы заявок (значки ✓⏳📋).
