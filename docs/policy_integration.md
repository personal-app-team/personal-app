# 🛡️ Интеграция политик и разрешений в Personal Management System

**Дата начала:** 24.12.2025  
**Последнее обновление:** 24.12.2025  
**Статус:** Активная разработка (начало работы с Filament Shield)

## 🎯 Цель проекта
Настроить полную систему авторизации с использованием Spatie Laravel Permission + Filament Shield + политик Laravel для 41 модели системы.

## 📊 Текущее состояние

### ✅ Выполнено:
- Установлен Filament Shield 3.9.10
- Настроены 6 базовых политик (Assignment, Shift, WorkRequest, User, Expense, Compensation)
- Созданы интеллектуальные сидеры PermissionSeeder и RoleSeeder
- Определены 11 ролей системы
- Начата настройка модели User с FilamentUser

### 🔄 В процессе:
- Настройка уровней доступа: FilamentUser → Resource Access → Policies
- Создание матрицы доступа 4×4 для всех ресурсов и ролей
- Интеграция Filament Shield как UI-надстройки

### 📈 Статистика:
- **Моделей:** 41
- **Filament Resources:** ~40 (нужно уточнить)
- **Политик создано:** 6/41 (15%)
- **Ролей:** 11
- **Разрешений в системе:** ~500 (генерируются PermissionSeeder)

## 🎯 ОБНОВЛЕННАЯ СТРАТЕГИЯ (с Filament Shield)

### 📋 Матрица доступа 4×4
Мы используем 4 типа доступа для каждого ресурса и роли:

| Тип доступа | Emoji | Описание | Что означает |
|-------------|-------|----------|--------------|
| **Нет доступа** | ❌ | Ресурс полностью скрыт | Не виден в навигации, нет доступа |
| **Только просмотр** | 👁️ | Read-only доступ | Можно просматривать записи, но нельзя изменять |
| **Полный доступ** | ✅ | CRUD операции | Просмотр, создание, редактирование, удаление |
| **Ограниченный доступ** | 🔐 | Кастомные правила | Специальная логика (только свои записи, подтверждение и т.д.) |

### 🔍 Таблица ограниченного доступа
Для каждого сочетания "ресурс × роль" с типом "ограниченный доступ" создается запись:

| Ресурс | Роль | Тип ограничения | Бизнес-правило | Нужные кастомные разрешения |
|--------|------|-----------------|----------------|---------------------------|
| Assignment | executor | Только свои записи | Видит и подтверждает только назначения где user_id = своему ID | `view_own_assignment`, `confirm_own_assignment` |
| Assignment | initiator | Частичный доступ | Видит все назначения, но создает только бригадиров | `view_any_assignment`, `create_brigadier_assignment` |
| ... | ... | ... | ... | ... |

### 🏗️ Архитектура системы разрешений (обновленная)

#### Четыре уровня контроля:
1. **Уровень панели (FilamentUser)** - Кто может войти в панель Filament?
2. **Уровень ресурса (Resource Access)** - Какие ресурсы видны в навигации?
3. **Уровень действий (Action Access)** - Какие кнопки/действия доступны?
4. **Уровень данных (Data Scope)** - Какие конкретные записи видны?

#### Компоненты системы:
- **FilamentUser interface** - контроль входа в панель
- **Filament Shield** - UI для управления ролями и разрешениями
- **Spatie Permissions** - хранилище и менеджер разрешений
- **Laravel Policies** - бизнес-логика доступа
- **Матрица доступа 4×4** - карта прав для всей системы

## 🚀 Текущий прогресс

### 1. Уровень 1: FilamentUser ✅
Настроен метод `canAccessPanel()` в модели User:
```php
public function canAccessPanel(Panel $panel): bool
{
    // Любой пользователь с хотя бы одной ролью может войти
    return $this->roles()->exists();
}
```

### 2. Уровень 2: Resource Access 🚧

В процессе создания матрицы доступа для всех ресурсов.

### 3. Интеграция Filament Shield:

* Shield установлен как UI для Spatie Permissions

* Отключена автоматическая генерация разрешений (используем PermissionSeeder)

* Будут созданы кастомные разрешения на основе матрицы

**📅 План реализации по матрице доступа**

**Фаза 1: Создание матрицы (1-2 дня)**

1. Получить список всех Filament Resources
2. Создать Google Sheets/CSV с матрицей 4×4
3. Заполнить матрицу для ключевых ресурсов (Assignment, WorkRequest, User, Shift)
4. Выявить случаи "ограниченного доступа"

**Фаза 2: Настройка кастомных разрешений (2-3 дня)**

1. Для каждого "ограниченного доступа" определить кастомные разрешения
2. Обновить PermissionSeeder с новыми разрешениями
3. Обновить RoleSeeder с назначением разрешений

**Фаза 3: Реализация политик (3-4 дня)**

1. Создать/обновить политики с бизнес-логикой
2. Настроить проверки в Filament Resources
3. Протестировать workflow для каждой роли

**Фаза 4: Тестирование и документирование (1-2 дня)**

1. Тестовые сценарии для всех ролей
2. Документация матрицы доступа
3. Обновление RoleSeeder и PermissionSeeder

**📋 Матрица доступа (шаблон)**

|Resource|admin|initiator|dispatcher|executor|contractor\_admin|contractor\_dispatcher|contractor\_executor|hr|manager|trainee|viewer|
| :- | :- | :- | :- | :- | :- | :- | :- | :- | :- | :- | :- |
|**UserResource**|✅|❌|🔐|❌|🔐|❌|❌|✅|❌|❌|❌|
|**AssignmentResource**|✅|🔐|✅|🔐|🔐|🔐|🔐|❌|👁️|❌|❌|
|**WorkRequestResource**|✅|✅|✅|👁️|❌|❌|❌|❌|👁️|❌|❌|
|**ShiftResource**|✅|👁️|🔐|🔐|🔐|🔐|🔐|❌|👁️|❌|❌|
|...|...|...|...|...|...|...|...|...|...|...|...|

**Легенда:** ❌ Нет доступа | 👁️ Только просмотр | ✅ Полный доступ | 🔐 Ограниченный доступ

## **🔧 Технические задачи**

### **1. Команда для списка ресурсов**
```bash
sail artisan make:command ListResources --command=resources:list
sail artisan resources:list > docs/resources\_matrix.txt
```

### **2. Шаблон матрицы в CSV**
```csv

Resource,admin,initiator,dispatcher,executor,contractor\_admin,contractor\_dispatcher,contractor\_executor,hr,manager,trainee,viewer,notes

UserResource,✅,❌,🔐,❌,🔐,❌,❌,✅,❌,❌,❌,dispatcher: только просмотр своих исполнителей

AssignmentResource,✅,🔐,✅,🔐,🔐,🔐,🔐,❌,👁️,❌,❌,executor: только свои назначения
```

### **3. Таблица ограниченного доступа**

## 📋 Таблица ограниченного доступа

### AssignmentResource

| Роль | Тип ограничения | Бизнес-правило | Кастомные разрешения |

|------|-----------------|----------------|---------------------|

| executor | Только свои | Видит и подтверждает только назначения где user\_id = своему ID | `view\_own\_assignment`, `confirm\_own\_assignment` |

| initiator | Частичный | Видит все назначения, создает только бригадиров | `view\_any\_assignment`, `create\_brigadier\_assignment` |

| contractor\_admin | Только своего подрядчика | Видит назначения только для своего подрядчика | `view\_contractor\_assignments` |

## **🔄 Цикл разработки (обновленный)**

### **Шаг 1: Анализ workflow модуля**
- Описание бизнес-логики доступа
- Определение типов доступа для ролей (4×4 матрица)
- Выявление случаев "ограниченного доступа"

### **Шаг 2: Заполнение матрицы доступа**
- Определение для каждой роли: ❌, 👁️, ✅, или 🔐
- Создание записей в "Таблице ограниченного доступа"

### **Шаг 3: Создание кастомных разрешений**
- На основе "Таблицы ограниченного доступа"
- Обновление PermissionSeeder
- Обновление RoleSeeder

### **Шаг 4: Реализация политик**
- Создание/обновление политик с бизнес-логикой
- Интеграция с Filament Resources

### **Шаг 5: Тестирование и документирование**
- Тестовые сценарии для всех ролей
- Обновление документации

## **📅 История изменений**

### **24.12.2025 - Внедрение матрицы доступа 4×4**
- Установлен Filament Shield
- Определена стратегия с 4 типами доступа
- Создан план реализации по матрице
- Начата настройка Уровня 1 (FilamentUser)

### **24.12.2025 - Начало проекта**
- Создан план интеграции
- Проанализированы текущие сидеры
- Определены приоритеты модулей

## **🚨 Важные заметки**

1. **Filament Shield** — это UI-надстройка над Spatie Permissions, а не замена
2. **Кастомные разрешения** создаются на основе "Таблицы ограниченного доступа"
3. **Матрица 4×4** — это карта прав, а не техническая реализация
4. **Тестировать постепенно** — начинать с ключевых ресурсов (Assignment, User, WorkRequest)

## **🎯 Следующие шаги**

1. **Создать команду** resources:list для получения списка всех ресурсов
2. **Создать Google Sheets** с матрицей доступа
3. **Заполнить матрицу** для первых 10 ресурсов
4. **Определить кастомные разрешения** для случаев "ограниченного доступа"
